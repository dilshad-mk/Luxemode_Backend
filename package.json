{
  "name": "luxemode-api",
  "version": "1.0.0",
  "description": "Backend API for Luxemode",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "author": "Dilshad",
  "license": "ISC",
  "dependencies": {
    "json-server": "0.17.4"
  }
}